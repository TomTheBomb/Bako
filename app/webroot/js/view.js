var recorderManager = TB.initRecorderManager(apiKey);
recorderManager.displayPlayer(archiveId, TOKEN, 'player', {width: 640, height: 480});
recorderManager.displayPlayer(archiveAnswerId, TOKEN, 'playerAnswer', {width: 640, height:480});

